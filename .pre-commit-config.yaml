# Pre-commit: block any commit that introduces credential-shaped secrets.
# Install:  pip install pre-commit && pre-commit install
repos:
  - repo: local
    hooks:
      - id: check-secrets
        name: check-secrets
        entry: python3 scripts/check_secrets.py
        language: system
        pass_filenames: false
        always_run: true
