name: task-stopped
description: >
  Deterministic heartbeat (no LLM) — agent finished a turn.
  Publishes a task.finished envelope (agent + team + snippet + task_ref +
  handoff) to the Office Redis bus.
events:
  - agent:end
