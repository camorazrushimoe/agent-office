name: task-accepted
description: >
  Deterministic heartbeat (no LLM) — agent began processing a message.
  Publishes a task.started envelope (agent + team + snippet + task_ref) to
  the Office Redis bus.
events:
  - agent:start
