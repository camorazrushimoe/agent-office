# Capability: foundation-smoke (change delta)

ADDED — full requirements live in `openspec/specs/foundation-smoke/spec.md`.

This change introduces the permanent capability and the first implementation
of the hierarchical smoke CLI for the Office shell.
